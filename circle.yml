machine:
  ruby:
    version: 2.3.1
  timezone:
    Asia/Tokyo

database:
  pre:
    - mv config/database.yml.ci config/database.yml
  override:
    - bundle exec rake db:migrate RAILS_ENV=test
    - bundle exec rake admins:add_admin RAILS_ENV=test
    - bundle exec rake db:seed RAILS_ENV=test