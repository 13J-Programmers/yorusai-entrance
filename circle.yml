machine:
  ruby:
    version: 2.3.1
  timezone:
    Asia/Tokyo

database:
  pre:
    - mv config/database.yml.ci config/database.yml
  override:
    - bundle exec rake db:migrate
    - bundle exec rake admins:add_admin
    - bundle exec rake db:seed