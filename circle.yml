machine:
  ruby:
    version: 2.3.1
  timezone:
    Asia/Tokyo

database:
  pre:
    - mv config/database.yml.ci config/database.yml
  override:
    - rails db:migrate
    - rails admins:add_admin
    - rails db:seed